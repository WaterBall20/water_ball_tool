# 项目开发规范 / Project Development Conventions

> 部分代码由 AI (deepseek-v4-pro) 生成 / Code partially generated by AI (deepseek-v4-pro)

---

## 一、模块与文件结构 / Module & File Structure

### 1.1 目录树

```
src/
├── main.rs                # 程序入口 / Entry point
├── lib.rs                 # 库根 / Library root (pub mod 声明)
├── command.rs             # CLI 命令实现 / CLI command implementations
├── command/
│   └── test.rs            # command 模块的全部测试 / All tests for command
├── tools.rs               # 通用工具函数 / General utility functions
├── file_finder.rs         # 文件搜索器主模块 / File finder main module
├── file_finder/
│   └── test.rs            # file_finder 模块的全部测试 / All tests for file_finder
├── wb_files_pack.rs       # 水球包文件模块入口 / WBFP module entry
├── wb_files_pack/
│   ├── data.rs            # 核心数据结构 / Core data structures
│   ├── manager.rs         # 包文件管理器 / Pack file manager
│   ├── manager/
│   │   └── test.rs        # manager 模块的全部测试 / All tests for manager
│   ├── allocator.rs       # 线程安全封装 / Thread-safe wrapper
│   ├── allocator/
│   │   └── test.rs        # allocator 模块的全部测试 / All tests for allocator
│   ├── pack_io.rs         # 底层文件 I/O / Low-level file I/O
│   ├── pack_io/
│   │   └── file.rs        # 虚拟文件读写器 / Virtual file reader-writer
│   ├── test.rs            # wb_files_pack 根模块测试 / Root module tests
│   └── net_server.rs      # 网络服务（开发中）/ Network server (WIP)
└── gakumasu/              # 游戏模拟器（开发中）/ Game simulator (WIP)
```

### 1.2 测试模块放置规则 / Test Module Placement Rules

| 规则                                           | 说明                                         |
|----------------------------------------------|--------------------------------------------|
| 每个 `.rs` 模块文件的测试**必须**放在同名目录的 `test.rs` 子模块中 | 如 `file_finder.rs` → `file_finder/test.rs` |
| 如果 `test.rs` 不存在，则创建它                        | `#[cfg(test)] mod test;` 声明在父模块底部          |
| **严禁**将测试放在父模块的 inline `mod test { }` 块中     | 必须使用独立文件                                   |
| **严禁**将测试放到其他模块的 test 目录                     | 每个模块的测试归自己管                                |
| 内部测试（逻辑验证）放模块自己的 `test.rs`                   | 直接调用被测函数                                   |
| 外部测试（集成/调用验证）放 `command/test.rs`             | 通过 CLI 命令入口调用                              |

**示例**：为 `file_finder.rs` 添加测试

```rust
// === file_finder.rs 底部 ===
#[cfg(test)]
mod test;  // 声明子模块，Rust 自动查找 file_finder/test.rs

// === file_finder/test.rs ===
use super::*;  // 引入父模块的所有公开项
// ... 测试代码 ...
```

---

## 二、命名规范 / Naming Conventions

### 2.1 变量/函数

| 原则                               | 示例                                                                    |
|----------------------------------|-----------------------------------------------------------------------|
| 禁止单字母缩写（循环变量除外）                  | ❌ `s`, `m`, `pb_c` → ✅ `skip_symlink`, `manager`, `progress_callback` |
| 禁止数字后缀区分同名函数                     | ❌ `create_file2`, `create_file3` → ✅ `create_file`, `create_file_raw` |
| 布尔变量用 `is_` / `has_` / `can_` 前缀 | ✅ `is_dirty`, `has_data`                                              |
| 函数名动词开头，描述行为                     | ✅ `copy_file_into_pack`, `search_files`                               |

### 2.2 常量

| 原则                   | 示例                                                          |
|----------------------|-------------------------------------------------------------|
| 避免缩写前缀（`s_`, `p_` 等） | ❌ `DEFAULT_S_MANIFEST_FILE` → ✅ `DEFAULT_SEPARATE_MANIFEST` |

---

## 三、注释规范 / Comment Conventions

### 3.1 文档注释

- 所有 `pub` 函数/结构体必须写 `///` Rust文档注释
- **中文在前，英文在后**（双行或同一段）
- 复杂逻辑处加行内注释说明"为什么这么做"

---

## 四、错误处理 / Error Handling

### 4.1 原则

| 场景         | 处理方式                                                 |
|------------|------------------------------------------------------|
| 可恢复错误      | 返回 `Result<_, Error>`                                |
| 不可恢复（逻辑错误） | `assert!` / `panic!`                                 |
| 命令行用户输入错误  | `eprintln!` + `process::exit(code)` **（不可用 panic!）** |

### 4.2 错误消息

- 中文在前，关键信息需要英文

---

## 五、锁与并发 / Locks & Concurrency

### 5.1 锁序

- 全局锁序：`manager (Mutex<WBFPManager>)` → `pack_io (Mutex<PackIO>)`
- **禁止**反向获取或嵌套获取同一把锁
- `PackFileWR::write()` 只允许锁 `pack_io`（写锁已在创建时前置）

### 5.2 线程安全

- 跨线程共享数据使用 `Arc<Mutex<T>>`
- 线程本地状态无需锁（如符号链接链 `InodeKey`）
- 元数据锁走状态机（`PackFileMetadataRun::try_lock` / `unlock`），不允许绕过

---

## 六、测试 / Tests

### 6.1 要求

- 每个公开模块必须有**内部测试**（所在目录 test.rs）和**外部测试**（command/test.rs）
- 测试之间不共享状态（每个测试独立创建 temp 目录并清理）
- 平台相关测试用 `#[cfg]` 适配，失败时 `return` 而非 `panic`

### 6.2 结构参考

```rust
// === file_finder/test.rs ===
use super::*;  // 必须

#[cfg(unix)]
fn platform_util() { ... }

#[cfg(windows)]
fn platform_util() { ... }

#[test]
fn test_case_one() { ... }
```

---

## 七、二进制格式相关 / Binary Format

### 7.1 数据块

- `DATA_BLOCK_LEN = 128`（清单数据块对齐）
- `DATA_DATA_BLOCK_LEN = 4 MiB`（数据块对齐）
- 整数使用**小端序**（LE）
- 布尔位序 **MSB-first**（位 0 = 最高位）

### 7.2 变更规则

- 修改二进制格式常量前**必须**同步更新 `docs/wb_files_pack/manifest-data.md`
- `ManifestDataBlock` 相关测试必须在 A/B 双块机制下验证

---

## 八、依赖 / Dependencies

### 8.1 不允许引入的依赖

- 任何 `unsafe` 代码块（除非有充分理由并加 `// SAFETY:` 注释）

### 8.2 现有的核心依赖

| 依赖                               | 用途   |
|----------------------------------|------|
| `serde` / `serde_json`           | 序列化  |
| `blake3`                         | 文件哈希 |
| `indicatif`                      | 进度条  |
| `tracing` / `tracing-subscriber` | 日志   |
| `sysinfo`                        | 进程检测 |
